import React from 'react';
import { Box, Typography } from '@mui/material';

const DealDetail: React.FC = () => {
    return (
        <Box>
            <Typography variant="h4">Deal Detail</Typography>
            <Typography variant="body1">Deal detail page coming soon...</Typography>
        </Box>
    );
};

export default DealDetail; 